export interface DeviceConfig {
  id : number,
  name : string,
  ipaddress : string,
  port : number,
  deviceId : number,
  isActive : boolean,
  lastSyncDate : string,
  isEditable: false,
  isSelected: false
}
